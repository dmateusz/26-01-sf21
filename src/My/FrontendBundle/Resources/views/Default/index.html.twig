{% extends "::layout.html.twig" %}

{% block content %}

<ul id="menu">
    {% for entity in entities %}
        <li>
            <a href="{{ path('song_show', { 'id': entity.id }) }}">
                    {{ entity }}
            </a>
        </li>
    {% endfor %}
</ul>

{% endblock %}
